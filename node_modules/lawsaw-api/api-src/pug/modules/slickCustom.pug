mixin slickCustom(options)
    -
        var newOptions = getOptions(options);
    .slickCustom(class= newOptions.id)
        if block
            block


mixin slickCustom-item(options)
    -
        var newOptions = getOptions(options);
    .slickCustom-item(class= newOptions.id)
        if block
            block