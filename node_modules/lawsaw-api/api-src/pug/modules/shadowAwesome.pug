mixin shadowAwesome(options)
    -
        var newOptions = getOptions(options),
        prefix = 'shadowAwesome-';
    .shadowAwesome(class= prefix+newOptions.type+' '+newOptions.className)
    .shadowAwesome-content(class= newOptions.contentClass)
        if block
            block